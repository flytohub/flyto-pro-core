# Package Boundary

This package owns reusable Flyto2 contracts, deterministic agent data, provider
ports, cost enforcement, workflow composition, configuration, and utility
primitives. Importing it must not start services, provision infrastructure, or
perform network calls. See the generated API inventory for every public symbol.
