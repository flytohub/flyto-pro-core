# Built-in Providers

Optional OpenAI and Qdrant adapters implement the public interface contracts.
They are unavailable without their extras and caller-supplied configuration;
method invocation may create external traffic and cost.
