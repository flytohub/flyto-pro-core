# Cost Control

Tracks estimated model cost, tokens, LLM calls, tool calls, iterations, runtime,
and warning thresholds. Pricing and budgets are caller-configurable; estimates
must not be presented as provider invoices.

The `atomic`, `micro`, `models`, `orchestrator`, `pipeline`, and `service`
subpackages are compatibility namespaces and currently export no behavior.
