# Observation

Collects structured browser, database, filesystem, network, runtime, step, and
module-I/O state into observation packets. Raw secrets and unrestricted file
contents must be filtered by the caller before persistence or model use.
