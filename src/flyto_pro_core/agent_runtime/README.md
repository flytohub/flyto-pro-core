# Agent Runtime

The agent runtime represents plans, observations, evidence, verification,
project state, interventions, error-memory patterns, and UI-facing progress as
versioned Python data. Scheduling and model reasoning belong to callers. See
`docs/AGENT_RUNTIME.md` for lifecycle and trust-boundary details.
