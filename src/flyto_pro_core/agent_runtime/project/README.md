# Project State

Models goals, tasks, steps, checklists, artifacts, project configuration, and
the local `.flyto/` directory layout. Managers perform explicit filesystem
operations only when invoked by a caller.
