# Runtime Integration

Adapters translate existing agent-loop, observation, and verification formats
to package contracts. Browser capture is asynchronous; filesystem hashing is
offloaded from the event loop. Adapters do not authenticate external systems.
