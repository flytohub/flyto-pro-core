# Core Utilities

Contains the service container, guarded access helpers, and fluent validators.
Container defaults instantiate optional providers only when their configuration
is present; no hosted lifecycle is owned here.
