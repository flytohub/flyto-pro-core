# Factory

Turns supported natural-language intents into deterministic Blueprint recipes,
composed steps, normalized references, canvas enrichment, and validation. Search
recall is filtered against original request terms so unsupported intents fail
instead of silently selecting unrelated blueprints.
