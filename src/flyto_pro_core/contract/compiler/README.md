# Workflow Compiler

Converts a validated workflow specification into ordered compiled nodes, port
bindings, routing rules, and an executable plan. Execution remains the
responsibility of Flyto2 Core.
