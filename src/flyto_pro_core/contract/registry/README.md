# Contract Registry

Loads current Flyto2 Core module metadata, applies local overrides, indexes
categories, and serves search, catalog, compatibility, connection, and migration
queries. The registry has no execution authority.
