# Workflow Validator

Checks duplicate IDs, modules, parameters, ports, edges, entry nodes, orphans,
connection limits, bindings, and schema compatibility. Results are structured
issues and edge diagnostics suitable for tools and user interfaces.
