# Contract Models

Defines data shapes, ports, parameter schemas, module contracts, workflow
nodes/edges, execution scopes, results, and traces. Flyto2 Core metadata aliases
and union parameter types are normalized at this boundary.
