# Contract Engine

The contract facade coordinates registry loading, workflow validation, binding
resolution, version checks, and executable-plan compilation. It validates and
compiles workflow specifications but never executes the resulting plan.
