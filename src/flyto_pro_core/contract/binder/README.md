# Binding Resolver

Builds available binding trees from workflow parameters, upstream outputs,
scope values, and loop context. It resolves expression syntax to typed binding
entries without evaluating arbitrary code.
