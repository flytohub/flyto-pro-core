# Configuration

Owns environment-backed dataclasses, constants, model selection, and timeout
lookups. YAML values are validated against known fields and environment values
take precedence. The generated environment catalog is the public configuration
contract.
