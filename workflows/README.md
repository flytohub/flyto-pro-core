# Contributor Workflows

These playbooks define repeatable repository work: idea capture, planning,
implementation, bug fixing, refactoring, investigation, and wrap-up. They are
process guidance rather than executable GitHub Actions. The source of truth for
automated checks is `.github/workflows/` and `scripts/verify.py`.
